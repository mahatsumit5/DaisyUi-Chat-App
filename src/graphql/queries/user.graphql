query LoggedInUser {
  data: loggedInUser {
    data {
      bio
      coverPicture
      email
      id
      fName
      lName
      isActive
      profile
    }

    message
    status
  }
}

mutation Logout($email: String!) {
  data: logout(email: $email) {
    status
    message
  }
}
